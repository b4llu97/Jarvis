# Jarvis AI - Environment Configuration Template
# Copy this file to config/.env and adjust values as needed

# General Settings
TZ=Europe/Zurich

# LLM Configuration
# OPENAI_API_KEY is required for using GPT-4, Claude, or Gemini via OpenAI-compatible API
# Leave empty to use only local Ollama
OPENAI_API_KEY=

# Primary LLM Provider: openai (for GPT-4/Claude/Gemini) or ollama (local)
PRIMARY_LLM_PROVIDER=openai
PRIMARY_LLM_MODEL=gpt-4.1-mini

# Fallback LLM Provider (used when primary fails)
FALLBACK_LLM_PROVIDER=ollama
FALLBACK_LLM_MODEL=llama3.1

# Legacy Settings (for backward compatibility)
LLAMA_ENDPOINT=http://llama:11434
CHROMA_ENDPOINT=http://chroma:8001
SQLITE_PATH=/app/data/facts.db

# ASR/TTS Settings
ASR_MODEL=small-int8
TTS_VOICE=de-DE

# Optional: Email Integration (for ingestion service)
# IMAP_SERVER=imap.gmail.com
# IMAP_USER=your-email@gmail.com
# IMAP_PASSWORD=your-app-password
# IMAP_FOLDER=INBOX

# Optional: Telegram Bot (for future use)
# TELEGRAM_BOT_TOKEN=
# TELEGRAM_CHAT_ID=

# Smart Home Integration (Home Assistant)
# HOME_ASSISTANT_URL: URL to your Home Assistant instance
# HOME_ASSISTANT_TOKEN: Long-Lived Access Token for authentication
# To create a token: Home Assistant -> Profile -> Long-Lived Access Tokens -> Create Token
HOME_ASSISTANT_URL=http://homeassistant.local:8123
HOME_ASSISTANT_TOKEN=
